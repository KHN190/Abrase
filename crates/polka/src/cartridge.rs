// Cartridge wire format: serialize `Module` to `.pk` bytes and read it back.
//
// TODO: not implemented. Tracked in refactor-crates.md; will land as a follow-up
// PR once the in-memory `Module` shape stabilises (string-constant interning,
// boxed-Int constants, etc.).
