effect Counter {
  op next() -> Int
}

fn work() -> <Counter> Int {
  let a = Counter.next();
  let b = Counter.next();
  a + b
}

fn main() -> Int {
  handle work() {
    Counter.next => {
      let a = resume(10);
      let b = resume(20);
      a + b
    }
    return v => v
  }
}
