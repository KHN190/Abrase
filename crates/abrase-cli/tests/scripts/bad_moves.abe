fn use_string(s: String) -> Int { 1 }

fn double_move() -> String {
  let s = "hi";
  let t = s;
  let u = s;
  u
}

fn use_after_move() -> Int {
  let s = "hi";
  let _ = use_string(s);
  use_string(s)
}

fn main() -> Int {
  let _ = double_move();
  use_after_move()
}
