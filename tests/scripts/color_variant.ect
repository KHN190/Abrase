type Color = Red | Green(Int) | Blue(Int, Int)

fn weight(c: Color) -> Int {
  match c {
    Red => 0
    Green(n) => n
    Blue(a, b) => a + b
    _ => -1
  }
}

fn main() -> Int {
  weight(Blue(10, 5))
}
